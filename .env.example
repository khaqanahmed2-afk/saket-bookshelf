# ==========
# SERVER CONFIG (Private - NEVER expose to client)
# ==========

# Database Setup
# Use the Transaction Pooler URL (usually port 6543) for best performance in serverless/lambdas, or Session (5432) for long running servers.
DATABASE_URL=postgresql://postgres:gZ%23%23XePL8U/X!.5@db.lmsjblphqjvhamkkwuto.supabase.co:5432/postgres
# Supabase Admin Access (For Backend API)
SUPABASE_URL=https://lmsjblphqjvhamkkwuto.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_-ngjIfGsdzY7VMTv4vYnKg_M8JhZDZg
# NOTE: Do NOT use the Anon Key here if you want to bypass RLS for admin tasks.

# Session Security
# Must be at least 32 chars. Use `openssl rand -hex 32` to generate.
SESSION_SECRET=abcdefghijklmnopqrstuvwxyz1234567890abcdef


# File Upload Limits
ADMIN_UPLOAD_MAX_MB=10

# Server Port
PORT=5000
NODE_ENV=production


# ==========
# CLIENT CONFIG (Public - Safe to expose)
# ==========

# Even though we don't use Supabase Client anymore, we keep these blank or placeholders
# just in case some legacy library expects them, or remove them entirely to be safe.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
